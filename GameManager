extends Node

var points = 0
var energy = 100
var strictness = 0

func add_points(amount):
	points += amount
	print("Points:", points)

func change_energy(amount):
	energy += amount
	print("Energy:", energy)

func change_strictness(amount):
	strictness += amount
	print("Strictness:", strictness)
